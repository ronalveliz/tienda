package com.backend.repository;

import com.backend.model.Orderss;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderssRepository extends JpaRepository<Orderss, Long> {
}